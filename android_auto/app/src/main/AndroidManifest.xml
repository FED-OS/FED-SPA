<?xml version="1.0" encoding="utf-8"?>
<!--
  FED-SPA Android Auto manifest.
  Licensed data ONLY - the subscriber envelope is not packaged here.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:theme="@style/AutoTheme">

        <activity
            android:name=".CarActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Voice search entry: "search FED-SPA parlors in Boynton Beach" -->
        <service
            android:name=".VoiceSearch"
            android:exported="false"
            android:permission="android.permission.BIND_VOICE_SEARCH">
            <intent-filter>
                <action android:name="android.search.action.GLOBAL_SEARCH" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service>

    </application>

</manifest>
