// Top-level build file. Plugin versions are pinned per the project's
// "no dependency drift" policy - bump once a year alongside the data refresh.

plugins {
    id 'com.android.application' version '8.2.2' apply false
    id 'org.jetbrains.kotlin.android' version '1.9.22' apply false
}
